@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

<div>
    <div>
        <h2>Categories</h2>
        <a asp-controller="CoverType" asp-action="Create">Create new Cover Type</a>
        @if(TempData["success"] != null)
        {
            <h2>@TempData["success"]</h2>
        }
    </div>
    <div>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Name</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model)
                {
                    <tr>
                        <td>@obj.Name</td>
                        <td><a asp-controller="CoverType" asp-action="Edit" asp-route-id="@obj.Id">Edit</a></td>
                        <td><a asp-controller="CoverType" asp-action="Delete" asp-route-id="@obj.Id">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

